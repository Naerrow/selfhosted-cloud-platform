# ADR-0006: 빌링 데이터 무결성 보장 방식

- **상태(Status)**: Proposed
- **결정일(Date)**: TBD (Phase 1 진입 전 확정)
- **결정자(Decider)**: 프로젝트 메인테이너
- **관련 ADR**: 0008 (Phase 3 빌링 스코프)

## Context — 맥락

ADR-0008은 빌링을 "결제 기능이 아닌 감사 대상 시스템"으로 다룬다고 명시했고, 그중 무결성 보장 메커니즘의 구체화를 본 ADR에 위임했다. 또한 내부 에이전트 지침의 빌링 코드 작성 규칙과 R-006(리스크 레지스터)은 빌링 무결성 결함을 **치명적** 영향으로 분류한다.

본 ADR은 다음을 결정한다.

- **append-only 미터링의 물리적 강제 방식**: 단순 DB 트리거 / WORM 스토리지 / 이벤트 로그 분리 중 어느 것인가?
- **무결성 증명 자료 구조**: 해시 체인(블록체인 유사) vs 머클 트리 vs 둘 다.
- **검증 가능성**: 외부 감사자가 청구서 단일 발행본만 보고 해당 기간 미터링·요율·합계의 무결성을 재계산 가능한가?
- **Idempotency 정의**: 같은 (테넌트, 기간) 입력에 대해 같은 청구서 해시가 재생산되는 함수 정의.
- **빌링 감사 로그 분리 보관**: 일반 운영 로그와 다른 스토리지·접근 제어.

본 ADR은 Phase 3 빌링 엔진 구현이 시작되기 전에 확정되어야 한다. Phase 1·2 단계에서는 본 ADR의 결정이 데이터 모델 설계 (Phase 1 기본 미터링)에 미리 영향을 준다.

## Decision — 결정

[Phase 1 진입 전 확정. 본 섹션은 결정 시점에 채워진다.]

## Alternatives Considered — 대안 비교

[Phase 1 진입 전 확정. 후보:]

| 대안 | 강점(예상) | 약점(예상) | 결정 |
|---|---|---|---|
| 해시 체인 (각 레코드가 직전 해시 포함) | 구현 단순, 검증 선형 | 부분 검증 시 전체 체인 필요 | TBD |
| 머클 트리 (기간 단위 루트) | 부분 검증·증명 효율 | 구현 복잡, 트리 재구성 비용 | TBD |
| 해시 체인 (개별 레코드) + 머클 루트 (기간 청구서) | 두 장점 조합 | 운영 복잡도 ↑ | TBD |
| 외부 timestamping (RFC 3161 / OpenTimestamps) | 외부 시점 보증 | 외부 서비스 의존, 셀프호스팅 정합 ↓ | TBD |
| 단순 DB append + 정기 백업 해시 | 가장 단순 | 위변조 탐지 약함 | 거부 (R-006 치명적) |

## Consequences — 결과

[결정 시점에 채워진다.]

## Open Questions

- 청구서 PDF에 무결성 증명(해시·머클 증명)을 어떻게 임베드하는가? 별도 부록 PDF인가?
- 단가 정책 변경 추적은 본 무결성 메커니즘과 같은 자료구조를 쓰는가, 별도인가?
- 보정 레코드(correction record)의 무결성은 어떻게 입증하는가? 원 레코드 → 보정 레코드 양방향 링크?
- 빌링 감사 로그의 보관 기간은 전자상거래법(5년) 등 법정 의무에 맞추는가? 컴플라이언스 매핑 문서와 연결.
- 결정의 회귀 트리거: Phase 7 Cloud Bursting 도입 시 AWS Cost Report 데이터의 무결성은 본 메커니즘과 어떻게 합쳐지는가?

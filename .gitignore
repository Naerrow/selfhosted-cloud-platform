# Internal-only planning/specification material.
# Keep public traceability docs tracked; put raw/private specs in these paths.
docs/private/
docs/internal/
docs/00-overview/requirements.raw.md
docs/00-overview/requirements.private.md
docs/00-overview/original-requirements.md
*.private.md
*.internal.md

# Internal-only files (not published; do not link from tracked docs).
CLAUDE.md
docs/00-overview/requirements.md

# OS·editor·tool artifacts.
.DS_Store
Thumbs.db
.claude/

# Secrets and credentials — never commit.
.env
.env.*
*.env
*.pem
*.key
*.p12
*.crt

# Encrypted runtime secrets (SOPS+age) — real values stay internal-only (ADR-0007).
# Public repo carries only the empty-value template (secrets.example.yaml) and
# the plaintext-free SOPS config (.sops.yaml); never the actual encrypted file.
secrets.enc.yaml
*.enc.yaml
*.enc.json
*.enc.env
